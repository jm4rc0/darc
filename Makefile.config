#Creating a file Makefile.config.local will allow some or all of these values to be overwritten.
#BASE=/home/ali/tmp/rtc
BASE=/rtc
BIN=$(BASE)/bin
LIB=$(BASE)/lib
PY=$(LIB)/python
IDL=$(BASE)/idl
ETC=$(BASE)/etc
SRC=$(BASE)/src
CONF=$(BASE)/conf
TEST=$(BASE)/test
INC=$(BASE)/include
DOC=$(BASE)/doc
#and include a local version if it exists
-include Makefile.config.local
